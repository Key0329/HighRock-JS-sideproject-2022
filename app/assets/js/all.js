// ---------------------- preloader ---------------------

window.onload = () => {
  const preLoader = document.querySelector('#preloader');
  if (preLoader) {
    preLoader.classList.add('loader-hide');
  }
};
